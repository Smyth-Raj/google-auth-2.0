<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Google OAuth with EJS</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @keyframes spin {
            0% {
                transform: rotate(0deg);
            }
            100% {
                transform: rotate(360deg);
            }
        }

        .spin-animation {
            animation: spin 5s infinite linear;
        }
    </style>
</head>
<body class="bg-gradient-to-r from-green-400 via-green-500 to-green-600">
    <div class="flex items-center justify-center min-h-screen">
        <div class="bg-white p-8 rounded-lg shadow-lg w-full sm:w-3/4 md:w-1/2 lg:w-1/3 bg-opacity-80 backdrop-blur-md">
            <h1 class="text-2xl font-semibold text-center text-gray-800 mb-6">Google Login </h1>

            <% if (!user) { %>
                <a href="/auth/google" class="block w-full bg-green-500 text-white py-3 text-center rounded-lg hover:bg-green-600 transition duration-200">Login with Google</a>
            <% } else { %>
                <div class="text-center">
                    <h2 class="text-xl font-semibold text-gray-800 mb-2">Welcome, <%= user.displayName %>!</h2>
                    <img src="<%= user.photos[0].value %>" alt="Profile Picture" class="mx-auto rounded-full mb-4 w-32 h-32 object-cover spin-animation">
                    <p class="text-gray-600 mb-4">Email: <%= user.emails[0].value %></p>
                    <a href="/logout" class="inline-block bg-red-500 text-white py-2 px-6 rounded-lg hover:bg-red-600 transition duration-200">Logout</a>
                </div>
            <% } %>
        </div>
    </div>

</body>
</html>
