<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @keyframes spin {
            0% {
                transform: rotate(0deg);
            }
            100% {
                transform: rotate(360deg);
            }
        }

        .spin-animation {
            animation: spin 5s infinite linear;
        }
    </style>
</head>
<body class="bg-gradient-to-r from-blue-400 via-blue-500 to-blue-600">
    <div class="flex items-center justify-center min-h-screen">
        <div class="bg-white p-8 rounded-lg shadow-lg w-full sm:w-3/4 md:w-1/2 lg:w-1/3 bg-opacity-80 backdrop-blur-md">
            <h1 class="text-3xl font-semibold text-center text-gray-800 mb-6">Welcome to Your Dashboard</h1>
            
            <div class="text-center mb-6">
                <h2 class="text-xl font-semibold text-gray-800">Hello, <%= user.name %>!</h2>
                <img src="<%= user.picture %>" alt="Profile Picture" class="mx-auto rounded-full mb-4 w-32 h-32 object-cover spin-animation">
                <p class="text-gray-600 mb-4">Email: <%= user.email %></p>
            </div>
            
            <div class="text-center mb-6">
                <a href="/profile" class="block bg-blue-500 text-white py-3 text-center rounded-lg hover:bg-blue-600 transition duration-200 mb-4">Go to Profile</a>
                <a href="/settings" class="block bg-green-500 text-white py-3 text-center rounded-lg hover:bg-green-600 transition duration-200 mb-4">Account Settings</a>
                <a href="/auth/google/logout" class="block bg-red-500 text-white py-3 text-center rounded-lg hover:bg-red-600 transition duration-200">Logout</a>
            </div>

            <div class="bg-gray-100 p-4 rounded-lg shadow-md">
                <h3 class="text-lg font-semibold text-gray-800">Quick Actions</h3>
                <ul class="list-none mt-2">
                    <li><a href="/create-post" class="text-blue-600 hover:text-blue-700">Create a new post</a></li>
                    <li><a href="/view-posts" class="text-blue-600 hover:text-blue-700">View posts</a></li>
                    <li><a href="/messages" class="text-blue-600 hover:text-blue-700">Messages</a></li>
                    <li><a href="/notifications" class="text-blue-600 hover:text-blue-700">Notifications</a></li>
                </ul>
            </div>
        </div>
    </div>
</body>
</html>
